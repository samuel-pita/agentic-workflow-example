# Tasks · Cimientos de la presentación

> Para la spec `01-spec.md` / plan `02-plan.md` (aprobado).
> Precondición ✅: skills `frontend-design`, `ui-ux-pro-max`, `web-design-guidelines` instaladas.

## Tasks
- [x] T1 — Crear `src/index.html` con `<head>` (viewport, `<title>`, favicons `../assets/…`, preconnect + Google Fonts Inter/JetBrains Mono/Space Grotesk, `css/styles.css`) y `<header>` con logo GIGA + `#mainTabs` con los 6 `.mtab[data-topic]` en orden y etiquetas cortas · satisface AC1, AC2, AC10 · verifica: abrir la página; el header muestra logo y 6 pestañas en el orden `Inicio·Introducción·Claude Code·Workflow·Prerequisitos·Caso`.
- [x] T2 — Añadir en `index.html` los 6 `<section class="panel" data-topic>` en orden, cada uno con **una** `.slide cover` (`.cover-index` + `.cover-title`, "Claude" del tema 00 en `<span class="accent">`), y la barra inferior (`#subTabs`, `#prevBtn`, `#nextBtn`, `#dots`) + `<script src="js/main.js" defer>` · satisface AC3 · verifica: cada tema tiene exactamente una portada con índice y título de la tabla, sin contenido interno.
- [x] T3 — Crear `src/css/styles.css` con `:root` (tokens `--brand #25c6a0`, `--ink #001622`, `--bg #fff`, grises/bordes derivados, `--font-title/-body/-mono`, espaciados/radios), reset ligero y `body { height:100vh; overflow:hidden; display:flex; flex-direction:column }` · satisface AC8, AC10 · verifica: no hay hex fuera de `:root`; el body no genera scroll.
- [x] T4 — Estilar header, `.mtab` (estado activo con acento), `#stage`, `.panel` (oculto salvo activo), `.slide` (oculto salvo activo, centrado) y `.slide.cover` (fondo `--ink`, texto claro, `.cover-index` mono, `.cover-title` Space Grotesk con `clamp()`, `.accent` verde) · satisface AC3, AC8 · verifica: la portada activa se ve centrada, fondo oscuro, "Claude" en verde, sin desbordar a 1280×720.
- [x] T5 — Estilar la barra inferior: `#subTabs` (chips de subpestaña), `#prevBtn`/`#nextBtn`, `#dots` · satisface AC4, AC5 · verifica: la barra se renderiza y los chips/dots tienen estado activo visible.
- [x] T6 — Añadir a `styles.css` el bloque `.pipe` completo (steps, tabs, stage, panels, `.active`, `.run`) respetando `prefers-reduced-motion` · satisface AC9 · verifica: un `.pipe` de prueba muestra el estilo del stepper.
- [x] T7 — Crear `src/js/main.js`: recolectar paneles y construir la lista global de slides (orden DOM) con `{topic,title,slug,globalIndex}` y un `slugify` compartido (kebab sin acentos) · satisface AC7 · verifica: en consola, la lista tiene 6 entradas con slugs limpios.
- [x] T8 — Implementar `render()`/`goTo`/`next`/`prev`: activa panel+slide, regenera `#subTabs` desde `data-title` del tema activo, marca `.mtab`/subpestaña/dot activos · satisface AC4, AC5 · verifica: cambiar de tema muestra su portada y `#subTabs` se llena solo (una chip "Portada" por tema).
- [x] T9 — Cablear interacciones de clic: `.mtab` → primer slide del tema, subpestaña → ese slide, `#prevBtn`/`#nextBtn`/`#dots` → recorrido global · satisface AC5 · verifica: clics recorren las 6 portadas en orden y prev/next respetan límites.
- [x] T10 — Añadir navegación por teclado (←/PageUp/Backspace = prev, →/Space/PageDown = next), ignorando `input`/`textarea`/`contenteditable` · satisface AC6 · verifica: las flechas cambian de portada; escribir en un input de prueba no navega.
- [x] T11 — Implementar deep-linking: hash `#topic/slug` con `replaceState` en cada `render()`, resolución al cargar y en `hashchange` (fallback: primer slide) · satisface AC7 · verifica: navegar actualiza el hash; recargar con `#workflow/portada` cae en esa portada.
- [x] T12 — Añadir el stepper `.pipe` genérico por delegación de eventos (`.pipe-tab[data-step]` activa su `.pipe-panel`, reinicia `.run`), independiente del hash · satisface AC9 · verifica: con un `.pipe` de prueba, clic en un paso cambia el panel; el paso no aparece en el hash.
- [x] T13 — Prueba temporal del `.pipe` (AC9): insertar un `.pipe` de prueba, verificar T6/T12, y **removerlo** (no commitear) · satisface AC9 · verifica: tras la prueba, ninguna portada contiene `.pipe`.
- [x] T14 — Validación final: `/verify` (servir desde raíz, recorrer los 10 AC) + `@content-auditor` + `@design-reviewer`; escribir hallazgos y fixes en `04-notas-implementacion.md` y actualizar la spec si algo cambió · satisface AC1–AC10 · verifica: los 10 criterios pasan y las notas quedan en el archivo `04`.
